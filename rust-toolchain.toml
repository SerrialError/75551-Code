[toolchain]
# Pinned to match the vexide-template (vexide 0.8.0) toolchain.
# The vexide target ships no prebuilt std, so rust-src is required for build-std.
channel = "nightly-2025-11-26"
components = ["rust-src"]
